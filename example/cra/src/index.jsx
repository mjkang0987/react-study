import React from 'react';
import ReactDOM from 'react-dom';
import UseRef from './App';

ReactDOM.render(
  <React.StrictMode>
    <UseRef />
  </React.StrictMode>,
  document.getElementById('root')
);